// when adding new environment keys, wrap the key in double quotes (").
// if the value of a key is a string, also use double quotes

export const environment = {
  "production": false,
  "hostServer": "http://brian.natelao.com",
  "serverPort": 3000
};
